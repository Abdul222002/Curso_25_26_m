

export default function createImageModal() {
  
    const imageModal = document.createElement("modal")
    
    // cerrar imagen modal
    // que sea click o ESC
    return {
        element, xxx
    }
}
